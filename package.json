{
  "name": "dorm-cleaning-reminder",
  "version": "1.0.0",
  "description": "2A211宿舍卫生值日提醒系统",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "cd frontend && npm install"
  },
  "dependencies": {
    "@upstash/redis": "^1.28.0"
  },
  "engines": {
    "node": ">=18"
  }
}
